using UnityEngine;

public class Product : MonoBehaviour
{
    /* create variables for each resource required to produce product e.g.
    public int lumberRequired { get; private set; }
    public int goldRequired { get; private set; }
    public int copperRequired { get; private set; }
    public bool lumberRequiredMet { get; private set; }
    public bool goldRequiredMet { get; private set; }
    public bool copperRequiredMet { get; private set; }
    public int lumberAdded { get; private set; }
    public int goldAdded { get; private set; }
    public int copperAdded { get; private set; }
    
    */

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
    
    //if requirements met, produce product
}
